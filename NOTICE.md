# Provenance

Original Luce Base HTTP/1.1 client, Copyright 2026 Dy Mokomi, MIT OR Apache-2.0.
Uses the Base `net` HTTP codecs. No libcurl or foreign TLS engine is linked.

This first slice speaks cleartext HTTP to numeric IPv4 loopback/peers. Verified
HTTPS belongs to `luce-tls` and is not implemented here.
